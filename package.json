{
  "name": "signal-flags.github.io",
  "version": "0.6.0",
  "description": "The signalflags.org website",
  "private": true,
  "scripts": {
    "build": "npm run lint:fix",
    "lint": "prettier . --check",
    "lint:fix": "prettier . --write"
  },
  "dependencies": {
    "prettier": "^2.2.1"
  }
}
